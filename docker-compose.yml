services:
  chess-coach:
    build:
      context: .
    container_name: chess-coach
    restart: unless-stopped

    network_mode: host

    env_file:
      - .env

    volumes:
      - ${CHESS_OUTPUT_DIR:-./chess_reviews}:/data
